[
  {
    mode = "n";
    key = "<leader>?";
    action._raw = "function() require('which-key').show({global=false})";
    options = {
      desc = "Buffer Local Keymaps (which-key)";
      silent = true;
    };
  }
]
